========================================================================
  Nexus AI MDM Platform {{ .Chart.AppVersion }} — Deployment Complete
========================================================================

Release:   {{ .Release.Name }}
Namespace: {{ include "nexus-mdm.namespace" . }}
Chart:     {{ .Chart.Name }}-{{ .Chart.Version }}

------------------------------------------------------------------------
  SERVICE ENDPOINTS (cluster-internal)
------------------------------------------------------------------------

  API Gateway       http://{{ include "nexus-mdm.fullname" . }}-api-gateway:{{ .Values.apiGateway.port }}
  MDM Core          http://{{ include "nexus-mdm.fullname" . }}-mdm-core:{{ .Values.mdmCore.port }}
  AI Service        http://{{ include "nexus-mdm.fullname" . }}-ai-service:{{ .Values.aiService.port }}
  Ingest Service    http://{{ include "nexus-mdm.fullname" . }}-ingest-service:{{ .Values.ingestService.port }}
  Policy Service    http://{{ include "nexus-mdm.fullname" . }}-policy-service:{{ .Values.policyService.port }}
  Search Service    http://{{ include "nexus-mdm.fullname" . }}-search-service:{{ .Values.searchService.port }}
  Notifications     http://{{ include "nexus-mdm.fullname" . }}-notification-service:{{ .Values.notificationService.port }}
  Distribution      http://{{ include "nexus-mdm.fullname" . }}-distribution-service:{{ .Values.distributionService.port }}
  Kafka Worker      (no HTTP — background outbox processor)

------------------------------------------------------------------------
  EXTERNAL ACCESS
------------------------------------------------------------------------

{{- if .Values.ingress.enabled }}
  {{- if .Values.ingress.tls.enabled }}
  API Gateway:  https://{{ .Values.ingress.host }}/
  WebSocket:    wss://{{ .Values.ingress.host }}/ws
  {{- else }}
  API Gateway:  http://{{ .Values.ingress.host }}/
  WebSocket:    ws://{{ .Values.ingress.host }}/ws
  {{- end }}
{{- else }}
  Ingress is disabled. Access the API Gateway by port-forwarding:

    kubectl port-forward -n {{ include "nexus-mdm.namespace" . }} \
      svc/{{ include "nexus-mdm.fullname" . }}-api-gateway {{ .Values.apiGateway.port }}:{{ .Values.apiGateway.port }}

  Then visit: http://localhost:{{ .Values.apiGateway.port }}/health
{{- end }}

------------------------------------------------------------------------
  HEALTH CHECKS
------------------------------------------------------------------------

  Verify all pods are running:

    kubectl get pods -n {{ include "nexus-mdm.namespace" . }}

  Check API Gateway health:

    kubectl exec -n {{ include "nexus-mdm.namespace" . }} \
      deploy/{{ include "nexus-mdm.fullname" . }}-api-gateway -- \
      wget -qO- http://localhost:{{ .Values.apiGateway.port }}/health

  Check MDM Core health (includes DB connectivity):

    kubectl exec -n {{ include "nexus-mdm.namespace" . }} \
      deploy/{{ include "nexus-mdm.fullname" . }}-mdm-core -- \
      wget -qO- http://localhost:{{ .Values.mdmCore.port }}/health/ready

------------------------------------------------------------------------
  SCALE A SERVICE
------------------------------------------------------------------------

  Manually override HPA bounds (temporary):

    kubectl scale deploy/{{ include "nexus-mdm.fullname" . }}-mdm-core \
      --replicas=5 -n {{ include "nexus-mdm.namespace" . }}

  Permanent change — update values and upgrade:

    helm upgrade {{ .Release.Name }} nexus-mdm/ \
      --set mdmCore.replicas=5 \
      -n {{ include "nexus-mdm.namespace" . }}

------------------------------------------------------------------------
  UPDATE SERVICE IMAGES
------------------------------------------------------------------------

    helm upgrade {{ .Release.Name }} nexus-mdm/ \
      --set apiGateway.image.tag=1.1.0 \
      --set mdmCore.image.tag=1.1.0 \
      --reuse-values \
      -n {{ include "nexus-mdm.namespace" . }}

------------------------------------------------------------------------
  VIEW LOGS
------------------------------------------------------------------------

    kubectl logs -n {{ include "nexus-mdm.namespace" . }} \
      -l app.kubernetes.io/component=api-gateway --tail=100 -f

    kubectl logs -n {{ include "nexus-mdm.namespace" . }} \
      -l app.kubernetes.io/component=kafka-event-service --tail=100 -f

------------------------------------------------------------------------
  ROLLBACK
------------------------------------------------------------------------

    helm rollback {{ .Release.Name }} -n {{ include "nexus-mdm.namespace" . }}

========================================================================
