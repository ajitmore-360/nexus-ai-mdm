{{/*
=============================================================================
Distribution Service — Deployment
Port: 8089
Owns: connector registry, distribution job queue, outbound data delivery
Requires: PostgreSQL
=============================================================================
*/}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nexus-mdm.fullname" . }}-{{ .Values.distributionService.name }}
  namespace: {{ include "nexus-mdm.namespace" . }}
  labels:
    {{- include "nexus-mdm.serviceLabels" (dict "root" . "service" .Values.distributionService.name) | nindent 4 }}
  annotations:
    deployment.kubernetes.io/revision: "1"
spec:
  replicas: {{ .Values.distributionService.replicas }}
  selector:
    matchLabels:
      {{- include "nexus-mdm.serviceSelectorLabels" (dict "root" . "service" .Values.distributionService.name) | nindent 6 }}
  strategy:
    type: {{ .Values.distributionService.strategy.type }}
    {{- if eq .Values.distributionService.strategy.type "RollingUpdate" }}
    rollingUpdate:
      maxSurge: {{ .Values.distributionService.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.distributionService.strategy.rollingUpdate.maxUnavailable }}
    {{- end }}
  template:
    metadata:
      labels:
        {{- include "nexus-mdm.serviceLabels" (dict "root" . "service" .Values.distributionService.name) | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: distribution-service
          image: "{{ tpl .Values.distributionService.image.repository . }}:{{ .Values.distributionService.image.tag }}"
          imagePullPolicy: {{ .Values.distributionService.image.pullPolicy | default .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.distributionService.port }}
              protocol: TCP
          env:
            - name: DISTRIBUTION_HOST
              value: "0.0.0.0"
            - name: DISTRIBUTION_PORT
              value: {{ .Values.distributionService.port | toString | quote }}
            - name: RUST_LOG
              valueFrom:
                configMapKeyRef:
                  name: {{ include "nexus-mdm.fullname" . }}-config
                  key: RUST_LOG
            - name: WORKER_POLL_INTERVAL_SECS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "nexus-mdm.fullname" . }}-config
                  key: WORKER_POLL_INTERVAL_SECS
            - name: WORKER_BATCH_SIZE
              valueFrom:
                configMapKeyRef:
                  name: {{ include "nexus-mdm.fullname" . }}-config
                  key: WORKER_BATCH_SIZE
            # ---- Secrets -----------------------------------------------------
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.existingSecret }}
                  key: {{ .Values.postgresql.databaseUrlKey }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.auth.existingSecret }}
                  key: {{ .Values.auth.jwtSecretKey }}
          livenessProbe:
            httpGet:
              path: {{ .Values.distributionService.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.distributionService.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.distributionService.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.distributionService.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.distributionService.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.distributionService.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.distributionService.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.distributionService.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.distributionService.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.distributionService.probes.readiness.failureThreshold }}
          resources:
            {{- include "nexus-mdm.resources" .Values.distributionService.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    {{- include "nexus-mdm.serviceSelectorLabels" (dict "root" . "service" .Values.distributionService.name) | nindent 20 }}
                topologyKey: kubernetes.io/hostname
