# =============================================================================
# Azile AI MDM Platform â€” Helm Values
# =============================================================================
# All values here are production-safe defaults.
# Override per-environment using --values overlays/production.yaml, etc.
# =============================================================================

# -----------------------------------------------------------------------------
# GLOBAL
# -----------------------------------------------------------------------------
global:
  imageRegistry: "ghcr.io/azile-mdm"
  imagePullPolicy: IfNotPresent
  namespace: azile-mdm
  # imagePullSecrets:
  #   - name: nexus-registry-credentials

# -----------------------------------------------------------------------------
# INGRESS
# -----------------------------------------------------------------------------
ingress:
  enabled: true
  className: nginx
  host: "api.azile-mdm.example.com"
  tls:
    enabled: true
    secretName: azile-mdm-tls
  annotations:
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
    cert-manager.io/cluster-issuer: letsencrypt-prod

# -----------------------------------------------------------------------------
# CERT-MANAGER (TLS via Let's Encrypt)
# -----------------------------------------------------------------------------
certManager:
  # Set to true once cert-manager is installed in the cluster.
  # Install: helm install cert-manager jetstack/cert-manager --set installCRDs=true
  enabled: true
  issuerName: letsencrypt-prod
  # Use https://acme-staging-v02.api.letsencrypt.org/directory for testing
  acmeServer: "https://acme-v02.api.letsencrypt.org/directory"
  # Replace with the ops team email â€” used for Let's Encrypt expiry notifications
  acmeEmail: "ops@azile-mdm.example.com"

# -----------------------------------------------------------------------------
# AUTHENTICATION
# -----------------------------------------------------------------------------
auth:
  # Reference to an existing Secret containing JWT_SECRET key
  existingSecret: azile-mdm-jwt-secret
  jwtSecretKey: JWT_SECRET
  # Token lifetime in seconds (24h default)
  tokenLifetimeSecs: 86400

# -----------------------------------------------------------------------------
# POSTGRESQL (external â€” deployed separately)
# -----------------------------------------------------------------------------
postgresql:
  host: azile-postgres
  port: 5432
  database: azile_mdm
  # Existing Secret must contain DATABASE_URL key
  existingSecret: azile-mdm-db-secret
  databaseUrlKey: DATABASE_URL
  # Connection pool settings (overridable per-service)
  maxConnections: 50
  minConnections: 5
  acquireTimeoutSecs: 10
  idleTimeoutSecs: 600
  maxLifetimeSecs: 1800

# -----------------------------------------------------------------------------
# REDIS (external â€” deployed separately)
# -----------------------------------------------------------------------------
redis:
  host: azile-redis
  port: 6379
  # Existing Secret must contain REDIS_URL key
  existingSecret: azile-mdm-redis-secret
  redisUrlKey: REDIS_URL
   # =============================================================================
# Azile AI MDM Platform â€” Helm Values
# =============================================================================
# All values here are production-safe defaults.
# Override per-environment using --values overlays/production.yaml, etc.
# =============================================================================

# -----------------------------------------------------------------------------
# GLOBAL
# -----------------------------------------------------------------------------
global:
  imageRegistry: "ghcr.io/azile-mdm"
  imagePullPolicy: IfNotPresent
  namespace: azile-mdm
  # imagePullSecrets:
  #   - name: nexus-registry-credentials

# -----------------------------------------------------------------------------
# INGRESS
# -----------------------------------------------------------------------------
ingress:
  enabled: true
  className: nginx
  host: "api.azile-mdm.example.com"
  tls:
    enabled: true
    secretName: azile-mdm-tls
  annotations:
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
    cert-manager.io/cluster-issuer: letsencrypt-prod

# -----------------------------------------------------------------------------
# CERT-MANAGER (TLS via Let's Encrypt)
# -----------------------------------------------------------------------------
certManager:
  # Set to true once cert-manager is installed in the cluster.
  # Install: helm install cert-manager jetstack/cert-manager --set installCRDs=true
  enabled: true
  issuerName: letsencrypt-prod
  # Use https://acme-staging-v02.api.letsencrypt.org/directory for testing
  acmeServer: "https://acme-v02.api.letsencrypt.org/directory"
  # Replace with the ops team email â€” used for Let's Encrypt expiry notifications
  acmeEmail: "ops@azile-mdm.example.com"

# -----------------------------------------------------------------------------
# AUTHENTICATION
# -----------------------------------------------------------------------------
auth:
  # Reference to an existing Secret containing JWT_SECRET key
  existingSecret: azile-mdm-jwt-secret
  jwtSecretKey: JWT_SECRET
  # Token lifetime in seconds (24h default)
  tokenLifetimeSecs: 86400

# -----------------------------------------------------------------------------
# POSTGRESQL (external â€” deployed separately)
# -----------------------------------------------------------------------------
postgresql:
  host: azile-postgres
  port: 5432
  database: azile_mdm
  # Existing Secret must contain DATABASE_URL key
  existingSecret: azile-mdm-db-secret
  databaseUrlKey: DATABASE_URL
  # Connection pool settings (overridable per-service)
  maxConnections: 50
  minConnections: 5
  acquireTimeoutSecs: 10
  idleTimeoutSecs: 600
  maxLifetimeSecs: 1800

# -----------------------------------------------------------------------------
# REDIS (external â€” deployed separately)
# -----------------------------------------------------------------------------
redis:
  host: azile-redis
  port: 6379
  # Existing Secret must contain REDIS_URL key
  existingSecret: azile-mdm-redis-secret
  redisUrlKey: REDIS_URL
  keyPrefix: "nexus:"
  # Rate limiter defaults (requests per window)
  rateLimitRequests: 100
  rateLimitWindowSecs: 60

# -----------------------------------------------------------------------------
# FIELD-LEVEL PII ENCRYPTION
# -----------------------------------------------------------------------------
fieldEncryption:
  # Existing Secret must contain a FIELD_ENCRYPTION_KEY entry (32-byte hex key).
  # Generate: openssl rand -hex 32
  # When externalSecrets.enabled=true the ExternalSecret controller creates this
  # secret automatically; otherwise create it manually before deploying:
  #   kubectl create secret generic azile-mdm-encryption-secret \
  #     --from-literal=FIELD_ENCRYPTION_KEY=$(openssl rand -hex 32)
  existingSecret: azile-mdm-encryption-secret
  encryptionKeyKey: FIELD_ENCRYPTION_KEY

# -----------------------------------------------------------------------------
# KAFKA (external â€” deployed separately)
# -----------------------------------------------------------------------------
kafka:
  brokers: "azile-kafka:9092"
  # Topics used by the platform
  topics:
    entityEvents: azile.entity.events
    matchEvents: azile.match.events
    auditLog: nexus.audit.log
    notifications: nexus.notifications
  # Consumer group prefix
  groupIdPrefix: "azile-mdm"

# -----------------------------------------------------------------------------
# OPA (external â€” deployed separately)
# -----------------------------------------------------------------------------
opa:
  url: "http://azile-opa:8181"
  timeoutSecs: 5

# -----------------------------------------------------------------------------
# OBSERVABILITY
# -----------------------------------------------------------------------------
observability:
  logLevel: info
  enableTracing: true
  # Set to your OTLP collector endpoint if using OpenTelemetry
  otlpEndpoint: ""

# =============================================================================
# SERVICE DEFINITIONS
# Each service section controls: image, replicas, resources, HPA, probes
# =============================================================================

# -----------------------------------------------------------------------------
# API GATEWAY  (port 8080)
# -----------------------------------------------------------------------------
apiGateway:
  name: api-gateway
  image:
    repository: "{{ .Values.global.imageRegistry }}/api-gateway"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8080
  env:
    GATEWAY_HOST: "0.0.0.0"
    GATEWAY_PORT: "8080"
    REQUEST_TIMEOUT_SECONDS: "30"
    MAX_RETRIES: "3"
    ALLOWED_ORIGINS: "https://app.azile-mdm.example.com"
    LOG_LEVEL: "info"
    ENABLE_TRACING: "true"
  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "1Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  # Pod disruption budget
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# MDM CORE  (port 8081)
# -----------------------------------------------------------------------------
mdmCore:
  name: mdm-core
  image:
    repository: "{{ .Values.global.imageRegistry }}/mdm-core"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8081
  env:
    MDM_CORE_HOST: "0.0.0.0"
    MDM_CORE_PORT: "8081"
    APP_ENV: "production"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
    limits:
      cpu: "2000m"
      memory: "2Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health/live
      initialDelaySeconds: 20
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health/ready
      initialDelaySeconds: 15
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# AI SERVICE  (port 8082)
# -----------------------------------------------------------------------------
aiService:
  name: ai-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/ai-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8082
  env:
    AI_SERVICE_PORT: "8082"
    AI_SERVICE_HOST: "0.0.0.0"
    # Ollama LLM backend â€” override with your cluster's Ollama endpoint
    OLLAMA_URL: "http://azile-ollama:11434"
    LLM_MODEL: "llama3.2"
    EMBED_MODEL: "nomic-embed-text"
    LLM_TEMPERATURE: "0.1"
    LLM_MAX_TOKENS: "4096"
    LLM_TIMEOUT_SECS: "120"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "4000m"
      memory: "4Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 10
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 10
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# INGEST SERVICE  (port 8083)
# -----------------------------------------------------------------------------
ingestService:
  name: ingest-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/ingest-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8083
  env:
    INGEST_PORT: "8083"
    INGEST_HOST: "0.0.0.0"
    BATCH_SIZE: "500"
    MAX_CONCURRENT_INGESTS: "10"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "1Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# POLICY SERVICE  (port 8084)
# -----------------------------------------------------------------------------
policyService:
  name: policy-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/policy-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8084
  env:
    POLICY_PORT: "8084"
    POLICY_HOST: "0.0.0.0"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# SEARCH SERVICE  (port 8085)
# -----------------------------------------------------------------------------
searchService:
  name: search-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/search-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8085
  env:
    SEARCH_PORT: "8085"
    SEARCH_HOST: "0.0.0.0"
    SEARCH_RESULT_LIMIT: "100"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "1Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# NOTIFICATION SERVICE  (port 8086)
# -----------------------------------------------------------------------------
notificationService:
  name: notification-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/notification-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8086
  env:
    NOTIFICATION_PORT: "8086"
    NOTIFICATION_HOST: "0.0.0.0"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# DISTRIBUTION SERVICE  (port 8089)
# -----------------------------------------------------------------------------
distributionService:
  name: distribution-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/distribution-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8089
  env:
    DISTRIBUTION_PORT: "8089"
    DISTRIBUTION_HOST: "0.0.0.0"
    WORKER_POLL_INTERVAL_SECS: "5"
    WORKER_BATCH_SIZE: "50"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# KAFKA EVENT SERVICE  (background worker + health HTTP on :8087)
# -----------------------------------------------------------------------------
kafkaEventService:
  name: kafka-event-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/kafka-event-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  # Workers are stateless outbox processors â€” 2 replicas for HA
  replicas: 2
  healthPort: 8087
  env:
    OUTBOX_POLL_INTERVAL_MS: "500"
    OUTBOX_BATCH_SIZE: "100"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# EXTERNAL SECRETS (optional â€” requires external-secrets operator)
# -----------------------------------------------------------------------------
# Install the operator before enabling:
#   helm repo add external-secrets https://charts.external-secrets.io
#   helm install external-secrets external-secrets/external-secrets \
#     --namespace external-secrets --create-namespace
#
# Supported providers: aws | vault
# When disabled, secrets must be pre-created manually (see templates/secrets.yaml).
# -----------------------------------------------------------------------------
externalSecrets:
  enabled: false
  provider: ""          # "aws" or "vault"
  refreshInterval: "1h"

  aws:
    region: ""           # e.g. "us-east-1"
    roleArn: ""          # IRSA role ARN; leave empty to use node IAM role
    secretPrefix: "azile-mdm"

  vault:
    server: ""           # e.g. "https://vault.internal:8200"
    path: "secret"
    version: "v2"
    secretPrefix: "azile-mdm"
    kubernetesMountPath: "kubernetes"
    kubernetesRole: "azile-mdm"
.Value -replace 'nexus', 'azile' :"
  # Rate limiter defaults (requests per window)
  rateLimitRequests: 100
  rateLimitWindowSecs: 60

# -----------------------------------------------------------------------------
# FIELD-LEVEL PII ENCRYPTION
# -----------------------------------------------------------------------------
fieldEncryption:
  # Existing Secret must contain a FIELD_ENCRYPTION_KEY entry (32-byte hex key).
  # Generate: openssl rand -hex 32
  # When externalSecrets.enabled=true the ExternalSecret controller creates this
  # secret automatically; otherwise create it manually before deploying:
  #   kubectl create secret generic azile-mdm-encryption-secret \
  #     --from-literal=FIELD_ENCRYPTION_KEY=$(openssl rand -hex 32)
  existingSecret: azile-mdm-encryption-secret
  encryptionKeyKey: FIELD_ENCRYPTION_KEY

# -----------------------------------------------------------------------------
# KAFKA (external â€” deployed separately)
# -----------------------------------------------------------------------------
kafka:
  brokers: "azile-kafka:9092"
  # Topics used by the platform
  topics:
    entityEvents: azile.entity.events
    matchEvents: azile.match.events
    auditLog: nexus.audit.log
    notifications: nexus.notifications
  # Consumer group prefix
  groupIdPrefix: "azile-mdm"

# -----------------------------------------------------------------------------
# OPA (external â€” deployed separately)
# -----------------------------------------------------------------------------
opa:
  url: "http://azile-opa:8181"
  timeoutSecs: 5

# -----------------------------------------------------------------------------
# OBSERVABILITY
# -----------------------------------------------------------------------------
observability:
  logLevel: info
  enableTracing: true
  # Set to your OTLP collector endpoint if using OpenTelemetry
  otlpEndpoint: ""

# =============================================================================
# SERVICE DEFINITIONS
# Each service section controls: image, replicas, resources, HPA, probes
# =============================================================================

# -----------------------------------------------------------------------------
# API GATEWAY  (port 8080)
# -----------------------------------------------------------------------------
apiGateway:
  name: api-gateway
  image:
    repository: "{{ .Values.global.imageRegistry }}/api-gateway"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8080
  env:
    GATEWAY_HOST: "0.0.0.0"
    GATEWAY_PORT: "8080"
    REQUEST_TIMEOUT_SECONDS: "30"
    MAX_RETRIES: "3"
    ALLOWED_ORIGINS: "https://app.azile-mdm.example.com"
    LOG_LEVEL: "info"
    ENABLE_TRACING: "true"
  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "1Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  # Pod disruption budget
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# MDM CORE  (port 8081)
# -----------------------------------------------------------------------------
mdmCore:
  name: mdm-core
  image:
    repository: "{{ .Values.global.imageRegistry }}/mdm-core"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8081
  env:
    MDM_CORE_HOST: "0.0.0.0"
    MDM_CORE_PORT: "8081"
    APP_ENV: "production"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
    limits:
      cpu: "2000m"
      memory: "2Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health/live
      initialDelaySeconds: 20
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health/ready
      initialDelaySeconds: 15
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# AI SERVICE  (port 8082)
# -----------------------------------------------------------------------------
aiService:
  name: ai-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/ai-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8082
  env:
    AI_SERVICE_PORT: "8082"
    AI_SERVICE_HOST: "0.0.0.0"
    # Ollama LLM backend â€” override with your cluster's Ollama endpoint
    OLLAMA_URL: "http://azile-ollama:11434"
    LLM_MODEL: "llama3.2"
    EMBED_MODEL: "nomic-embed-text"
    LLM_TEMPERATURE: "0.1"
    LLM_MAX_TOKENS: "4096"
    LLM_TIMEOUT_SECS: "120"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "4000m"
      memory: "4Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 10
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 10
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# INGEST SERVICE  (port 8083)
# -----------------------------------------------------------------------------
ingestService:
  name: ingest-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/ingest-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8083
  env:
    INGEST_PORT: "8083"
    INGEST_HOST: "0.0.0.0"
    BATCH_SIZE: "500"
    MAX_CONCURRENT_INGESTS: "10"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "1Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# POLICY SERVICE  (port 8084)
# -----------------------------------------------------------------------------
policyService:
  name: policy-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/policy-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8084
  env:
    POLICY_PORT: "8084"
    POLICY_HOST: "0.0.0.0"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# SEARCH SERVICE  (port 8085)
# -----------------------------------------------------------------------------
searchService:
  name: search-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/search-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8085
  env:
    SEARCH_PORT: "8085"
    SEARCH_HOST: "0.0.0.0"
    SEARCH_RESULT_LIMIT: "100"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "1Gi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# NOTIFICATION SERVICE  (port 8086)
# -----------------------------------------------------------------------------
notificationService:
  name: notification-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/notification-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8086
  env:
    NOTIFICATION_PORT: "8086"
    NOTIFICATION_HOST: "0.0.0.0"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# DISTRIBUTION SERVICE  (port 8089)
# -----------------------------------------------------------------------------
distributionService:
  name: distribution-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/distribution-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  replicas: 2
  port: 8089
  env:
    DISTRIBUTION_PORT: "8089"
    DISTRIBUTION_HOST: "0.0.0.0"
    WORKER_POLL_INTERVAL_SECS: "5"
    WORKER_BATCH_SIZE: "50"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  probes:
    liveness:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 5
      failureThreshold: 3
    readiness:
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# KAFKA EVENT SERVICE  (background worker + health HTTP on :8087)
# -----------------------------------------------------------------------------
kafkaEventService:
  name: kafka-event-service
  image:
    repository: "{{ .Values.global.imageRegistry }}/kafka-event-service"
    tag: "1.0.0"
    pullPolicy: "{{ .Values.global.imagePullPolicy }}"
  # Workers are stateless outbox processors â€” 2 replicas for HA
  replicas: 2
  healthPort: 8087
  env:
    OUTBOX_POLL_INTERVAL_MS: "500"
    OUTBOX_BATCH_SIZE: "100"
    RUST_LOG: "info"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  pdb:
    enabled: true
    minAvailable: 1

# -----------------------------------------------------------------------------
# EXTERNAL SECRETS (optional â€” requires external-secrets operator)
# -----------------------------------------------------------------------------
# Install the operator before enabling:
#   helm repo add external-secrets https://charts.external-secrets.io
#   helm install external-secrets external-secrets/external-secrets \
#     --namespace external-secrets --create-namespace
#
# Supported providers: aws | vault
# When disabled, secrets must be pre-created manually (see templates/secrets.yaml).
# -----------------------------------------------------------------------------
externalSecrets:
  enabled: false
  provider: ""          # "aws" or "vault"
  refreshInterval: "1h"

  aws:
    region: ""           # e.g. "us-east-1"
    roleArn: ""          # IRSA role ARN; leave empty to use node IAM role
    secretPrefix: "azile-mdm"

  vault:
    server: ""           # e.g. "https://vault.internal:8200"
    path: "secret"
    version: "v2"
    secretPrefix: "azile-mdm"
    kubernetesMountPath: "kubernetes"
    kubernetesRole: "azile-mdm"
