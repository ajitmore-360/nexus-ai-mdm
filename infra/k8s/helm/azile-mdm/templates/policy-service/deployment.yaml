{{/*
=============================================================================
Policy Service â€” Deployment
Port: 8084
Owns: policy evaluation, OPA integration, GDPR compliance engine, rule CRUD
Requires: PostgreSQL, OPA
=============================================================================
*/}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "azile-mdm.fullname" . }}-{{ .Values.policyService.name }}
  namespace: {{ include "azile-mdm.namespace" . }}
  labels:
    {{- include "azile-mdm.serviceLabels" (dict "root" . "service" .Values.policyService.name) | nindent 4 }}
  annotations:
    deployment.kubernetes.io/revision: "1"
spec:
  replicas: {{ .Values.policyService.replicas }}
  selector:
    matchLabels:
      {{- include "azile-mdm.serviceSelectorLabels" (dict "root" . "service" .Values.policyService.name) | nindent 6 }}
  strategy:
    type: {{ .Values.policyService.strategy.type }}
    {{- if eq .Values.policyService.strategy.type "RollingUpdate" }}
    rollingUpdate:
      maxSurge: {{ .Values.policyService.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.policyService.strategy.rollingUpdate.maxUnavailable }}
    {{- end }}
  template:
    metadata:
      labels:
        {{- include "azile-mdm.serviceLabels" (dict "root" . "service" .Values.policyService.name) | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: policy-service
          image: "{{ tpl .Values.policyService.image.repository . }}:{{ .Values.policyService.image.tag }}"
          imagePullPolicy: {{ .Values.policyService.image.pullPolicy | default .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.policyService.port }}
              protocol: TCP
          env:
            - name: POLICY_HOST
              value: "0.0.0.0"
            - name: POLICY_PORT
              value: {{ .Values.policyService.port | toString | quote }}
            - name: RUST_LOG
              valueFrom:
                configMapKeyRef:
                  name: {{ include "azile-mdm.fullname" . }}-config
                  key: RUST_LOG
            - name: OPA_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "azile-mdm.fullname" . }}-config
                  key: OPA_URL
            - name: OPA_TIMEOUT_SECS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "azile-mdm.fullname" . }}-config
                  key: OPA_TIMEOUT_SECS
            # ---- Secrets -----------------------------------------------------
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.existingSecret }}
                  key: {{ .Values.postgresql.databaseUrlKey }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.auth.existingSecret }}
                  key: {{ .Values.auth.jwtSecretKey }}
          livenessProbe:
            httpGet:
              path: {{ .Values.policyService.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.policyService.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.policyService.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.policyService.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.policyService.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.policyService.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.policyService.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.policyService.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.policyService.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.policyService.probes.readiness.failureThreshold }}
          resources:
            {{- include "azile-mdm.resources" .Values.policyService.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    {{- include "azile-mdm.serviceSelectorLabels" (dict "root" . "service" .Values.policyService.name) | nindent 20 }}
                topologyKey: kubernetes.io/hostname
