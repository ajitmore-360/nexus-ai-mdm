apiVersion: v1
kind: Service
metadata:
  name: {{ include "azile-mdm.fullname" . }}-{{ .Values.aiService.name }}
  namespace: {{ include "azile-mdm.namespace" . }}
  labels:
    {{- include "azile-mdm.serviceLabels" (dict "root" . "service" .Values.aiService.name) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "azile-mdm.serviceSelectorLabels" (dict "root" . "service" .Values.aiService.name) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.aiService.port }}
      targetPort: http
      protocol: TCP
