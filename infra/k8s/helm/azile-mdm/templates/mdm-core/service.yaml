apiVersion: v1
kind: Service
metadata:
  name: {{ include "azile-mdm.fullname" . }}-{{ .Values.mdmCore.name }}
  namespace: {{ include "azile-mdm.namespace" . }}
  labels:
    {{- include "azile-mdm.serviceLabels" (dict "root" . "service" .Values.mdmCore.name) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "azile-mdm.serviceSelectorLabels" (dict "root" . "service" .Values.mdmCore.name) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.mdmCore.port }}
      targetPort: http
      protocol: TCP
