apiVersion: v1
kind: Service
metadata:
  name: {{ include "azile-mdm.fullname" . }}-{{ .Values.ingestService.name }}
  namespace: {{ include "azile-mdm.namespace" . }}
  labels:
    {{- include "azile-mdm.serviceLabels" (dict "root" . "service" .Values.ingestService.name) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "azile-mdm.serviceSelectorLabels" (dict "root" . "service" .Values.ingestService.name) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.ingestService.port }}
      targetPort: http
      protocol: TCP
